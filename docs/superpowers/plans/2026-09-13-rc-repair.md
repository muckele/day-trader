# RC safety repair and supported runtime implementation plan

**Goal:** Repair RC-001/RC-002, preserve prior evidence, and verify the combined paper-only candidate on supported Node24.

**Architecture:** Extend existing canonical intent/account coordination. Complete provider preflight before a database-only final dispatch claim serialized with control generation and caller lease. Keep claimed-but-unobserved work durable and report draining until resolved. Version portfolio observations against canonical fills; refuse unknown coverage and restore headroom through coherent reconciliation. No broker writes in transaction callbacks and no second ledger.

**Spec:** User repair request dated2026-09-13; preserved independent review in docs/evidence/rc-review. Existing master brief remains operator-owned untracked documentation.

- [x] Preserve reviewed reports/probes/logs after secret inspection; record hashes and provenance.
- [x] Convert original account-GET and stale-position probes to asserting tests; record red source/runtime/executable/commands/state.
- [x] RC-001: adapter final authorization, canonical dispatch claim, serialized settings generation/worker lease, protection/close lease contract; truthful API/UI drain status.
- [x] RC-002: versioned observation + canonical exposure continuity, delayed-fill coverage refusal and positive reconciliation; full origin/partial/replacement/retry matrix.
- [x] Add stable mandatory RC scenario identities and omission/skip/failure verifier regressions.
- [ ] Run targeted and complete safety verification; inspect changes and commit safety repair (separate fixes where practical; shared lifecycle coordination may require one coherent safety commit).
- [ ] RUNTIME-001 separate commit: fresh official Node24 patch selection, both images, CI/verifier/local pin/engines/docs; consistent Node/npm.
- [ ] Verify full combined candidate, Linux images, isolated production readiness/static navigation, authorized audits and available image checks.
- [ ] Independent code review, secret/diff checks, exact checkpoint/acceptance/evidence updates and intended-only local commits. No push/PR/deploy/external brokerage/SMTP/inference/credential changes.

Required stop ordering: S-before-D prohibits dispatch. D-before-S with T unknown stays potentially in flight; no lease expiry/404/time-based inference of no-send. Healthy responding callers reconcile/cancel to terminal and drain completes. Disable retains deliberate owner manual and protective/reducing permissions.

Required exposure rule: cash-spend conversion never removes position risk. Unknown observed-position/known-fill coverage blocks new openings; confirmed coverage must restore legitimate headroom without duplicate contributions. Each transaction retry validates fresh versioned inputs; static-price regressions demonstrate admission limits, not market-price guarantees.
